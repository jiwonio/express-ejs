/**
 * Simple greeting function that logs to console
 * @param {string} name - Name of the person to greet
 */
export function showGreeting(name) {
  const message = `Hello, ${name}! This message is from a JavaScript module.`;
  console.log(message);
}